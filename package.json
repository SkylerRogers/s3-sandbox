{
  "name": "s3-sandbox",
  "version": "1.0.0",
  "description": "S3 thangz",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "upload": "node --env-file=dev.env upload.js",
    "download": "node --env-file=dev.env download.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.637.0"
  }
}
